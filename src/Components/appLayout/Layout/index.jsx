import { Link, Outlet, useLoaderData } from "react-router-dom"
import styles from "./index.module.scss"

export const Layout = () => {

    return (

        <>
            <br />
            <Outlet />

        </>
    )

} 